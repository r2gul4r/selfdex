# Selfdex Run: node-only-selfdex-doctor

- status: local_verified
- project_key: selfdex
- task: Make `selfdex doctor` run a Node-native setup check by default.
- reason: The Python-free public path should start by removing Python from the user-facing setup status check while keeping advanced Python tooling as fallback.

## Changes

- `bin/selfdex.js` now runs a Node-native doctor by default.
- `selfdex doctor --python <path>` still delegates to the legacy Python doctor.
- Node-native doctor checks:
  - plugin directory
  - marketplace entry
  - global `selfdex` skill
  - installed root config
  - project-scoped Codex subagent policy files
  - Git availability
  - GitHub and ChatGPT Apps recommended integration hints
  - GPT Pro manual account status
  - Gmail-not-required boundary
- `tests/test_npm_cli.py` covers the Node-native doctor and the explicit legacy Python fallback.
- README Korean/English docs now state that default `selfdex doctor` does not require Python.

## Verification

- `python -m unittest tests.test_npm_cli`: pass, 8 tests.
- `node bin/selfdex.js doctor --install-root C:\Users\Administrator\selfdex --format json`: pass, runtime `node-native`.
- `node bin/selfdex.js doctor --install-root C:\Users\Administrator\selfdex --format markdown`: pass.
- `python scripts/check_doc_drift.py --root . --format json`: pass.
- `python scripts/check_campaign_budget.py --root . --include-git-diff --format json`: pass.
- `git diff --check`: pass with CRLF warnings only.

## Notes

- The installer still uses Python for the plugin installer in this slice.
- The next Python-free slice should target the installer/plugin-copy path or the read-only planner path.
